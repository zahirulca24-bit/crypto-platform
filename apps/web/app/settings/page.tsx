"use client"
import { API_BASE_URL } from "@/lib/api"; import { PageShell, Panel } from "@/components/page-shell"
export default function SettingsPage(){return <PageShell title="Settings" subtitle="Frontend runtime configuration. Sensitive exchange credentials are not exposed in this UI."><div className="grid gap-6 xl:grid-cols-2"><Panel title="API Configuration"><div className="space-y-3 text-sm"><div><div className="text-xs uppercase tracking-wider text-slate-500">NEXT_PUBLIC_API_BASE_URL</div><code className="mt-1 block rounded bg-black/30 p-3 text-cyan-200">{API_BASE_URL}</code></div><p className="text-slate-400">Set this environment variable at build/runtime deployment configuration. For Render, use the HTTPS API origin supplied by your backend service.</p></div></Panel><Panel title="Trading Safety"><p className="text-sm text-slate-300">This frontend does not contain exchange credentials, does not add Live approval, and does not change the Phase-2 Risk Engine boundary. Candidate <code>approved_for_demo</code> remains research governance status only.</p></Panel></div></PageShell>}
